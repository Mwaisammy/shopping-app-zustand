// import { create } from "zustand";

// type PurchaseType = {
//   purchase: number;
//   addPurchase: () => void;
//   removePurchase: () => void;
// };

// export const usePurchaseStore = create<PurchaseType>((set) => ({
//   purchase: 0,
//   addPurchase: () => set((state) => ({ purchase: state.purchase + 1 })),
//   removePurchase: () => set((state) => ({ purchase: state.purchase - 1 })),
// }));
